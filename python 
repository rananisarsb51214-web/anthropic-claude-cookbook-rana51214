import base64

with open("image.png", "rb") as image_file:
    image_data = base64.b64encode(image_file.read()).decode("utf-8")

response = client.messages.create(
    model="claude-sonnet-4-20250514",
    max_tokens=1024,
    messages=[
        {
            "role": "user",
            "content": [
                {
                    "type": "image",
                    "source": {
                        "type": "base64",
                        "media_type": "image/png",
                        "data": image_data,
                    },
                },
                {
                    "type": "text",
                    "text": "Analyze this architecture diagram"
                }
            ]
        }
    ]
)response = client.messages.create(
    model="claude-sonnet-4-20250514",
    max_tokens=1024,
    messages=[
        {
            "role": "user",
            "content": "Return deployment JSON schema"
        }
    ]
)import time
from anthropic import RateLimitError

MAX_RETRIES = 5

for attempt in range(MAX_RETRIES):
    try:
        response = client.messages.create(
            model="claude-sonnet-4-20250514",
            max_tokens=512,
            messages=[
                {
                    "role": "user",
                    "content": "Analyze infrastructure"
                }
            ]
        )

        break

    except RateLimitError:
        wait_time = 2 ** attempt
        time.sleep(wait_time)with client.messages.stream(
    model="claude-sonnet-4-20250514",
    max_tokens=1024,
    messages=[
        {
            "role": "user",
            "content": "Stream AI responses"
        }
    ]
) as stream:
    for text in stream.text_stream:
        print(text, end="", flush=True)from anthropic import Anthropic

client = Anthropic(
    api_key="whsec_P2hWteHoCcimLXd3sY3wiYhGE7Oh/"
)

response = client.messages.create(
    model="claude-sonnet-4-20250514",
    max_tokens=1024,
    messages=[
        {
            "role": "user",
            "content": "Build a scalable AI workflow"
        }
    ]
)

print(response.content)https://claude.ai/public/artifacts/ac60473f-85e6-4958-8b3f-29fee57dc9ab